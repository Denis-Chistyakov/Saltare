# Secrets for API keys
# NOTE: Replace base64 encoded values with your actual keys
# To encode: echo -n "your-key" | base64
apiVersion: v1
kind: Secret
metadata:
  name: saltare-secrets
  namespace: saltare
  labels:
    app.kubernetes.io/name: saltare
type: Opaque
data:
  # Cerebras API Key (primary LLM)
  # echo -n "csk-your-cerebras-key" | base64
  cerebras-api-key: Y3NrLXlvdXItY2VyZWJyYXMta2V5

  # OpenRouter API Key (fallback LLM + embeddings)
  # echo -n "sk-or-v1-your-openrouter-key" | base64
  openrouter-api-key: c2stb3ItdjEteW91ci1vcGVucm91dGVyLWtleQ==

  # Meilisearch Master Key
  # echo -n "your-meilisearch-master-key" | base64
  meilisearch-api-key: eW91ci1tZWlsaXNlYXJjaC1tYXN0ZXIta2V5

  # Typesense API Key (if using Typesense)
  # echo -n "your-typesense-key" | base64
  typesense-api-key: eW91ci10eXBlc2Vuc2Uta2V5

